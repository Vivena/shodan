#define BLOCK_SIZE 1024;




// --------------------------
// Définition des structures

typedef struct {
  char octets[BLOCK_SIZE]; // tableau contenant 
  int free; // nombre d'octets disponibles dans le block
} block;
